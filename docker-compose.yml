services:
  gcp_service:
    container_name: gcp_service
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/apps/gcp_service/keys/key.json
    volumes:
      - .:/apps/gcp_service
    stdin_open: true
    tty: true
