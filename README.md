# Setup Guide
- **Copy `.env.example` to `.env` & fill up values below**
```
```
- **Setup GCloud service acc key**
    - Go to `GCP > Service Account > Manage Keys`
    - Get the `keys.json` file
    - Run `cli/gcp_key.sh`

- **Start up container via `docker compose up -d`**